version: "3.8"

services:
  aosr-web:
    build: .
    container_name: word-template-generator-web
    restart: unless-stopped
    ports:
      - "${AOSR_PORT:-8080}:8080"
    volumes:
      - ${AOSR_WORKSPACE_PATH:-./workspace}:/workspace
