version: "3.8"

services:
  word-template-web:
    build: .
    container_name: word-template-generator-web
    restart: unless-stopped
    ports:
      - "${WTG_PORT:-8080}:8080"
    volumes:
      - ${WTG_WORKSPACE_PATH:-./workspace}:/workspace
